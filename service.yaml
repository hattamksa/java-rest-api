apiVersion: v1
kind: Service
metadata:
  name: java-rest-jreendpoint
  labels:
    app: java-rest-jreendpoint
spec:
  selector:
    app: java-rest-jreendpoint
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
