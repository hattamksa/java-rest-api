apiVersion: v1
kind: Service
metadata:
  name: java-rest-api
  labels:
    app: java-rest-api
spec:
  selector:
    app: java-rest-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
